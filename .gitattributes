# Normalise line endings so the repo stays clean regardless of who checks it out.
* text=auto eol=lf

# PowerShell wants CRLF on Windows.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

*.jar binary
*.png binary
*.ogg binary
*.zip binary
